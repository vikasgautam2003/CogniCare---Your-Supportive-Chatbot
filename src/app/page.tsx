import ChatPage from './components/ChatPage';

const Home = () => {
  return (
    <main className="w-full h-screen bg-gradient-to-br from-blue-50 via-white to-cyan-50 font-sans flex items-center justify-center p-4">
      <ChatPage />
    </main>
  );
};

export default Home;
