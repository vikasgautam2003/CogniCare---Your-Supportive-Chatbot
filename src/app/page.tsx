import ChatPage from './components/ChatPage';

const Home = () => {
  return (
    <main className="w-full h-screen bg-gradient-to-br from-gray-900 via-slate-900 to-black font-sans flex items-center justify-center">
      <ChatPage />
    </main>
  );
};

export default Home;
